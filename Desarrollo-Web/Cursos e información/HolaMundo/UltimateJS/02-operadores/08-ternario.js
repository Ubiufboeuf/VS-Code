// expresión ? 'si es true' : 'si es false' => el operador Elvis en algunos lenguajes
let edad = 25;
let acceso = edad > 17 ? 'Permitir ingreso' : 'Denegar ingreso';

console.log(acceso);

// Es muy parecido a un if - else sin líneas